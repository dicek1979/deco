<script type="text/javascript">
</script>

<h1><%= t('views.user_menu.user_management') %></h1>
<div onclick="open_modal_window('/user_management/show_moderate/1', 'modal');">koko</div>
<div class="modal"></div>
<div class="comment">
  <div class="table-address-a">
    <div class="item-top">
      <%= t('activerecord.attributes.users.login') %>
    </div>
    <div class="input-top">
      <%= current_user.login %>
    </div>
    <div class="item-mid">
      <%= t('activerecord.attributes.users.name') %>
    </div>
    <div class="input-mid">
      <%= current_user.name %>
    </div>
    <div class="item-mid">
      <%= t('activerecord.attributes.users.email') %>
    </div>
    <div class="input-mid">
      <%= current_user.email %>
    </div>
    <div class="item-mid">
      <%= t('activerecord.attributes.users.moderate_id') %>

    </div>
    <div class="input-mid">
      <% if current_user.moderate.present? %>
      <div class="input-l">名前： <%= current_user.moderate.name %></div>
      <div class="input-l">種別：
        <% if current_user.moderate.type_flag == 1 %>
          <%= t('views.moderate.simple') %>
        <% else %>
          <%= t('views.moderate.nomal') %>
        <% end %>
      </div>
      <div class="input-l">ルートメンバー：</div>
        <% moderater_count = 0 %>
        <% for moderater in current_user.moderate.moderaters %>
          <% if moderater.user.present? %>
            <% unless moderater_count == 0 %>
            <br>
            <% end %>
            &nbsp;&nbsp;・<%= moderater.user.name %>（<%= moderater.user.login %>）
          <% moderater_count += 1 %>
          <% end %>
        <% end %>
      <% else %>
        なし
      <% end %>
    </div>
    <div class="item-bot">
      <%= t('.moderate_route_include_myself') %>
    </div>
    <div class="input-bot">
<% current_user.moderaters.each do |x| %>
  <%#= x.moderate.moderaters %>
<%= x.moderate.name %>:

        <% moderater_count = 0 %>
        <% for moderater in x.moderate.moderaters %>
            <% unless moderater_count == 0 %>
            <br>
            <% end %>
            &nbsp;&nbsp;・<%= moderater.user.name %>（<%= moderater.user.login %>）
          <% moderater_count += 1 %>
        <% end %>
<% end %>

    </div>


  </div>
</div>
